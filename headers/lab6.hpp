#include <iostream>
#pragma once

using namespace mt {
	bool equal_columns(int n, int(&arr)[m][m])

	bool isPrime(int x)

	int sum(int n, int(&arr)[m][m], int i)

	void swap_rows(int n, int(&arr)[m][m], int i, int j)

	void sort(int n, int(&arr)[m][m])
}